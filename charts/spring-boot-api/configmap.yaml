apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "spring-boot-api.fullname" . }}-config
data:
  config.json: |
    {
      "key": "value",
      "nested": { "nested_key": "nested_value" }
    }